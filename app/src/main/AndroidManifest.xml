<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.evaluation">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/app_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/app_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.Evaluation">
        <activity android:name=".controller.DetailsWalll"></activity>
        <activity android:name=".controller.DetailsWall" />
        <activity android:name=".controller.DetailsTerrasse" />
        <activity android:name=".controller.DetailsWindow" />
        <activity android:name=".controller.HomeActivity" />
        <activity android:name=".controller.LoginActivity" />
        <activity android:name=".controller.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>